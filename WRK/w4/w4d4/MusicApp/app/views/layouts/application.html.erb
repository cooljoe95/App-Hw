<!DOCTYPE html>
<html>
<head>
  <title>MusicApp</title>
  <%= csrf_meta_tags %>
</head>
<body>
  <!-- ... -->
  <% if !current_user.nil? %>
    <%= button_to "Logout", session_url, method: :delete %>
    Logged in as: <%= current_user.email %>
  <% end %>

  <%= yield %>

  </body>
  </html>
